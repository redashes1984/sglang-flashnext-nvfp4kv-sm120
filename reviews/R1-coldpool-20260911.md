# R1 审计 — expert cold pool v2.9 + PLE sidecar + identity gate

审计对象：/root/sglang-fl-nvfp4 @ 50b9c33（仓库快照即生产，md5 一致性由星野核验）。
方法：增量审查（仅审冷池栈相关 hunks），静态阅读 + 单测覆盖比对；本机无 torch，单测未复跑（CT112 侧 T1-T9 已有全绿记录，见 commit f5a61bc/afd0504）。只读，未改任何文件。

## 🔴 阻断问题（必须修复）

无。核对过的高危面均干净：
- shrink→capture 时序：model_runner.py:1145-1148 在 loader 返回后、graph capture（initialize 流程后置）之前完成 SHRUNK，且 draft worker 被排除 ✓
- staging 全程在 graph 外（model_runner.py:1630-1633 位于 forward 尾部 + patches/expert_cold_pool.py:733 capture 中直接 return），同流顺序保证可见性 ✓
- pinned buffer 生命周期：flat 视图钉住 pbuf，del buf（patches/expert_cold_pool.py:552-586）后旧层存储由 pool.host 引用链保活，失败行经 _return_row 归还（:245-250），无静默 slot 泄漏 ✓
- alias guard：未别名化的 swizzled scale 拒绝装载而非静默降级（patches/expert_cold_pool.py:463-473）✓
- fail-loud 路径：inventory gate raise（:532-537）、identity gate 缺 config raise（:267-272）、部署脚本锚点 count≠1 exit(2)（scripts/deploy_expert_cold_pool.py:40-47）✓
- id() 复用防护：_CFG_REFS 钉住注册对象（:274），T9 已覆盖死配置与 forged 对象的碰撞场景 ✓

## 🟡 重要问题（强烈建议）

1. remap_topk_ids 缺上限钳制，融合共享专家 id 会越界索引 — patches/expert_cold_pool.py:302-304
   `safe = topk_ids.clamp(min=0).long()` 后直接 `tbl[safe]`；tbl numel==width==num_experts。当 num_fused_shared_experts>0 时，_post_process_topk_ids 内 fused_append_shared_experts（runtime-src/topk.py:2051-2058，base id=N）先于 remap 执行，共享列 id ≥ width → gather 越界（CUDA device assert / CPU IndexError）。对照同文件 packed 路径已做 `clamp(max=tbl.numel()-1)`（:317），两条路径不对称。当前 TP1/trtllm 部署大概率 num_fused_shared=0 而休眠，但任何开融合的后续配置会首次 forward 即炸。修法：范围掩码——`ok = safe < tbl.numel()`，`tbl[safe.clamp(max=tbl.numel()-1)]` 后经 `torch.where(ok, mapped, topk_ids)` 透传共享列。stash_demand 侧同理由对共享列做范围过滤（现在是 clamp(max=width-1)，:335-337，会把共享列算进最后一名专家的需求统计）。
2. （并入上条的次要项）after_forward_hook 里 `_env_int` 外再套一层 try/except/pass（patches/expert_cold_pool.py:743-745）冗余——_env_int 自身已吞 ValueError（:60-64）。若 env 配了非法值会静默回落默认 10，属轻度静默降级面；建议至少在回落时 debug 打一行。

## 🟢 改进建议（可选）

1. scripts/deploy_expert_cold_pool.py 中 v2.9 门控文本三处重复：UPG29 dict（:60-82）、keep-mask-gate 锚点 new 块（:105-125）、stash-remap-exit new 块（:142-160），加上文件尾部 packed 逻辑实际共四份表达。升级时改一漏三的概率高；建议抽成常量单点声明。锚点机制本身经核对是幂等且 fail-loud 的（upgrade→skip→MISS 链路完整），漂移风险主要在文本重复上。
2. patches/expert_cold_pool.py:122 `_LEAK_DUMPED` 声明后无任何使用，v2.5b 取证遗留，可删。
3. PleFilePrefetcher.enqueue 依赖 `.cpu()` 同步整条流来算页集（runtime-src/qwen4_exp_ple_table.py:125），注释承认 ~1s 级开销；后续可改为 host 侧预取 id 列或用 non_blocking copy + event，decode 路径已由行数下限豁免，影响可控，暂不催。

## ℹ️ 风格提示（仅供参考）

- tests/test_cold_pool_logic.py:32-33 FakeParam 类定义后未用。
- PLE 模块注释密度与信息量高（backends 语义、崩溃安全顺序、madvise 分块理由），属正向样本，无需动。

## 结论

代码栈整体自证充分：capture/shrink 时序、alias/未别名拒绝、identity gate + 引用钉、checksum 自检、staging 失败归还行，全部有注释级设计理由 + 对应单测（T1-T9）覆盖。主要刀口集中在 remap 范围钳制的不对称（🟡）与部署脚本文本四写一处的维护漂移（🟢）。

评分：**60/100**（🔴×0 −0，🟡×1 −20，🟢×3 −30，ℹ️ −0）。未达 95 门槛：必修复项 = 🟡-1（含 stash 范围过滤）；修复后可复审上调。
