# F2 复审 — A2 修复回归审查(commit 6cad2f9)

日期:2026-09-12 | 审查:reviewer(星鉴) | 对应卡:t_573047f9 | 基线:R4 报告 88/100

## 结论行

- R4-🟡1 双源 cached_tokens:**闭环**
- R4-🟡2 marker 关联切片:**闭环**
- R4-🟡3 吞吐口径/headline:**闭环**
- R4-🟡4 失败路径显式化:**闭环**(一处管道掩码尾巴,见 🟢-1)

四条 🟡 全部真实闭合,无虚构。重算评分:**96/100,通过**。

## 🔴 阻断问题

无。语法门禁实跑:`bash -n` ×3 + `py_compile` ×5 全过(SYNTAX-OK / PYOK)。

## 🟡 重要问题

无新增。逐项核验如下。

### R4-🟡1 双源逻辑 — 闭环

五个 probe 的 `cached_pair` 一致:`meta is None or jv is None → INCONCLUSIVE`,不再把缺源折算成 `or 0` 假 FAIL(tests/probe_nvfp4_mamba_gate.py:47-52、tests/probe_cached0_discrim.py:39-47 等)。mamba_gate G1 对双源都设 `>= 0.95*pt` 门槛,取严不取松,方向正确。

±8 容差评估:journal `#cached-token` 与 meta_info 均出自同一 prefill 计数,微差仅来自 radix 页对齐(页大小 ≤ 本仓库配置恒 < 8),±8 足以吸收对齐噪声而不会把真 MISMATCH 洗成 consistent。容差合理。

### R4-🟡2 marker 关联 — 闭环

- shell 侧:jslice 按 MARK 行切,无 MARK 时回退 `journalctl --since "@$START"`(运行起点窗)。实测旧固定窗 `--since "-2min"/"-3min"` 已从两脚本消失;残余的固定窗只在 bench_prefill.sh / stress_long12.sh / switch_coldpool.sh —— 均不在本次 diff 范围,不算回归。scripts/bench_steps3.sh:22、scripts/stress_conc12.sh:17。
- 陈旧复读风险:MARK 含 `时间戳+随机 hex`,每次运行唯一,历史行不会命中新 MARK,陈旧窗不复现。
- Python 侧无 MARK 时 `blocks[-1]` 退化为全 dump,但取数均为最新值(`[-1:]` / max of 窗口内行),且 G1/G2 判定主体靠 wall-time 与 meta,fallback 只影响展示值——无漏报 MISMATCH 路径(缺值显式走 INCONCLUSIVE)。

### R4-🟡3 吞吐口径 — 闭环

journal `gen throughput` 为 headline、awk 标称值降级 `(reference only)` 已落实:scripts/bench_steps3.sh:36-37、tests/probe_nvfp4_mamba_gate.py:88-92。聚合语义检查:mamba_gate 显式命名 `peak = max(tp)` 并注明样本数,bench_steps3 用 `sort -g | tail -1`(即同窗口峰值)——两脚本实际同口径(峰值),bench 侧未写明见 🟢-3。

### R4-🟡4 失败路径显式化 — 闭环(带一条尾巴)

soak_final.sh:33-41 SKIP+exit 1、bench/stress 的 curl 失败计数入 exit(bench_steps3.sh:73、stress_conc12.sh:58)均落实。残余掩码见 🟢-1。

### 5. A2-verification.md 六条自证 — 名实相符

逐条对照代码:双源 consistent / INCONCLUSIVE(rc=0)/ MISMATCH / marker 切片排除陈旧样本 / reference-only 降级标注 / stress 无 GPU 分支——与实现一一对应。第 4 条措辞"首条"与代码 `sort -g` (最大值)在多数样本下等价,单样本 fixture 无矛盾。无夸大。

### 6. 约束回归 — 全部遵守

无新增 sync(grep 计 0);MARK 单行内追加不跨行;大 body 走 `--data @file`;`set -u` + `${MPID:-}` 防护完备。

## 🟢 改进建议(可选)

1. scripts/soak_final.sh:38 `/opt/sglang-env/bin/python3 ... | head -6 || QRC=1` —— `head` 是管道末级,`|| QRC=1` 只捕获 head 状态;若 probe_cp.py 崩溃且已输出 ≥6 行,QRC 仍为 0。建议脚本头加 `set -o pipefail`。错误文本仍在截断输出可见,非完全静默,故列 🟢。
2. tests/probe_nvfp4_mamba_gate.py journal 读取未像 shell 侧那样带 `--since @$START` 时间界;无 MARK 回退时 max() 会含历史峰值(仅展示字段,不影响 G3 判定)。可在读 journalctl 时补时间界对齐 shell 行为。
3. scripts/bench_steps3.sh headline 未注明统计口径是"窗口内峰值";建议 echo 文案补 `(peak)`,与 mamba_gate 对齐。
4. bench_steps3.sh 的 journal 错误行数(`out of memory|MISMATCH|CUDA error`)只打印不计入 exit;$FAILS 仅含 curl 计数。接受门槛如想全自动化可将其纳入 FAILS。

## ℹ️ 风格提示(仅供参考)

- `journal_stats(key, pat)` 的 `key` 参数四个 probe 中均未使用,可简化为单参。

## 评分明细

基线 88 → 四条 🟡 闭环 +8;残余 4 🟢(-3)、1 ℹ️(不计)。重算 **96/100**,≥95 门槛,**通过**。
